# Local development environment settings

# Node environment
NODE_ENV=development

# API settings
PORT=3000

# Grafana Loki configuration (for logging in cloud deployment)
# GRAFANA_LOKI_URL=https://logs-prod-xx-xxx.grafana.net/loki/api/v1/push 
# GRAFANA_API_KEY=your-grafana-api-key

# Metrics configuration
# Options: 'grafana' for Grafana Cloud, 'local' for local Prometheus, or leave empty to disable metric pushing
METRICS_PROVIDER=local 

# Local Prometheus Pushgateway
# This URL should point to your local Prometheus Pushgateway
# If running via the local-monitoring docker-compose, use:
LOCAL_PUSHGATEWAY_URL=http://localhost:9091/metrics/job/todo-api

# GRAFANA_USERNAME=your-grafana-username
# GRAFANA_PASSWORD=your-grafana-password 